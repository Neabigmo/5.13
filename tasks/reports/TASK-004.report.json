

## TASK-004 Complete ✅

### Summary

Implemented deterministic tie-breaking helpers per the frozen spec (`02_DEFINITIONS_SPEC.md`):

**Implementation** (`src/knn_stability/tie_breaking.py`):
- `order_neighbors_by_distance_and_index()` — Lexicographic ordering by (distance, index)
- `break_label_tie()` — Label vote tie-breaking favoring 0 ≺ 1
- `select_k_neighbors()` — k-neighbor selection with deterministic ordering
- `compute_majority_vote()` — Majority vote with tie-breaking

**Tests** (`tests/test_tie_breaking.py`):
- 38 new tests covering distance ties, label vote ties, k-boundary scenarios, edge cases
- All 90 tests pass

### Changed Files
| File | Change |
|------|--------|
| `src/knn_stability/tie_breaking.py` | +207 lines |
| `tests/test_tie_breaking.py` | +303 lines |

### Commands
```bash
python -m pytest tests/test_tie_breaking.py -v  # 38 passed
python -m pytest -v                              # 90 passed
```

### Report
Full report saved to `tasks/reports/TASK-004.md`.

---

**Ready for Codex review.** To commit with task ID:
```bash
git add src/knn_stability/tie_breaking.py tests/test_tie_breaking.py tasks/reports/TASK-004.md
git commit -m "TASK-004: Implement deterministic tie-breaking"
```
